# Clone this file and rename the clone to `git-init.sh`
# if you want to use this template for configuring the global git.
git config --global user.email "user.email@minimall.io"
git config --global user.name "minimall-user.name"
git config --global core.editor "code"
git config --global alias.co commit
git config --global alias.ch checkout 
git config --global alias.br branch 
git config --global alias.re restore 
git config --global alias.st status
