git config --local user.email "user.email@minimall.io"
git config --local user.name "minimall-user.name"
git config --local core.editor "code"
git config --local alias.co commit
git config --local alias.ch checkout 
git config --local alias.br branch 
git config --local alias.re restore 
git config --local alias.st status
